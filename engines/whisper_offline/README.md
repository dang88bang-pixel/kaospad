# Offline Whisper + Reim-Matrix

This directory is reserved for quantized on-device speech recognition assets and
SQLite rhyme dictionaries. CI uses deterministic placeholder manifests generated
by `engines/neurallift_360/scripts/download_weights.py`; no cloud API or token is
required.
